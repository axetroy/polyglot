{
  "name": "@polyglot/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "polyglot": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --outDir dist --tsconfig tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
