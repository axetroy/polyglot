{
  "name": "@polyglot/formats-jpeg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --outDir dist --tsconfig tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
